{
  "buildCommand": "npm install && chmod +x node_modules/.bin/vite && node node_modules/vite/bin/vite.js build",
  "outputDirectory": "dist",
  "framework": "vue",
  "crons": [
    {
      "path": "/api/auto-sync",
      "schedule": "0 */6 * * *"
    }
  ]
}